import SwiftUI

struct ReportView: View {
    var body: some View {
        NavigationStack {
            Text("レポート")
                .navigationTitle("レポート")
        }
    }
}

#Preview {
    ReportView()
}
